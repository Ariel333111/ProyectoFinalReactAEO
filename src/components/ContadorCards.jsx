/* 
import { useState } from "react";
import { Container, Button } from "react-bootstrap";

function Contador() {
  const [contador, setcontador] = useState(0);

  return (
    <Container>
      <Button OnClik={() => setcontador(contador + 1)} variant="primary">
        Agregar al Carrito
      </Button>
    </Container>
  );
}

export default Contador;
*/
